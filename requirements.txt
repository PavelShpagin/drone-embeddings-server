# Drone Embeddings Server - New Architecture Dependencies
# =====================================================

# Core web server
fastapi>=0.95.0,<0.110.0
uvicorn[standard]>=0.20.0,<0.25.0
pydantic>=1.10.0,<3.0.0

# Image processing and computer vision
Pillow>=9.0.0,<11.0.0
numpy>=1.21.0,<2.0.0
opencv-python-headless>=4.7.0

# Enhanced logging and visualization (NEW)
matplotlib>=3.7.0
pandas>=1.5.0
seaborn>=0.12.0  # Optional: for better plots

# Deep learning models - CPU-only for AWS deployment
# torch>=1.13.0,<2.1.0
# torchvision>=0.14.0,<0.16.0

# Google Earth Engine for satellite imagery
earthengine-api>=0.1.300

# HTTP requests and utilities
requests>=2.25.0
retry>=0.9.2

# File handling and compression (built-in zipfile module used)

# Additional dependencies for compatibility
typing-extensions>=4.0.0
python-multipart>=0.0.9

# For better Pydantic compatibility
pydantic-core>=2.10.0,<2.35.0

# Production deployment (optional)
# gunicorn>=20.1.0

# Development and testing (optional)
# pytest>=7.0.0
# pytest-asyncio>=0.20.0
websockets