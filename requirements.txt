# Satellite Embedding Server Dependencies - AWS Compatible
# Core web server (compatible with older Python/pip)
fastapi>=0.95.0,<0.105.0
uvicorn[standard]>=0.20.0,<0.25.0
pydantic>=1.10.0,<3.0.0

# Image processing
Pillow>=9.0.0,<11.0.0
numpy>=1.21.0,<2.0.0

# Deep learning models - CPU-only for AWS deployment  
# torch>=1.13.0,<2.1.0
# torchvision>=0.14.0,<0.16.0

# Google Earth Engine
earthengine-api>=0.1.300

# HTTP requests and utilities
requests>=2.25.0
retry>=0.9.2

# Additional dependencies for compatibility
typing-extensions>=4.0.0
# Data serialization - pickle is built-in for Python 3.8+
# pickle5>=0.0.12; python_version < "3.8"

# Optional: For production deployment (FastAPI works better with uvicorn)
# gunicorn>=20.1.0

# JSON handling (built-in)
# json - built-in module

# System utilities (built-in)
# os, sys, pathlib, uuid, time, math, threading - built-in modules
